// Blank desktop — what we route to when a route-window is closed.
// Renders nothing so the user lands on an empty desktop (wallpaper + dock + menu bar).
export default function DesktopPage() {
  return null;
}
